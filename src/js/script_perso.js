// Génère moi la structure d'un fichier jquery

$(document).ready(function() {
    // Code à exécuter une fois que le DOM est chargé
    console.log('Le DOM est chargé !');

    gsap.to('.container_exo_gsap h1', {x: 500});
});